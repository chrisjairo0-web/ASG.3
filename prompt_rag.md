Assignment: The RAG Concept Demo
Topic: Foundation Models, Hallucination, and RAG. Task: Foundation models are brittle and prone to hallucinations. Retrieval-Augmented Generation (RAG) is an engineering approach for connecting models to specific knowledge. Requirements:
Feed an engineering manual or a technical paper into an AI coding agent (like Claude Code) or an AI notebook (like NotebookLM).
Design a rapid prototype or a simulated workflow demonstrating chunking, embedding, and retrieval strategies.
Deliverable: A working Python demo (e.g., a simple Gradio/Streamlit app generated by AI) OR a comprehensive Audio/Video overview demonstrating how your RAG pipeline extracts precise answers grounded in retrieved evidence while reducing hallucinations.


**Prompt:**

"You are an expert AI-Native Architect. I need you to build a working Python demo of a three-stage Retrieval-Augmented Generation (RAG) pipeline to connect a language model to specific engineering knowledge and reduce hallucinations through grounded retrieval.

Please generate the Python code for a modular project with the following file structure and requirements:

**1. `src/indexing.py` (Offline and Upload Ingestion Pipeline)**
*   Create functions to support both ingestion modes:
*   Load and index a bundled default PDF located at `data/document.pdf` for an offline demo path.
*   Accept a user-uploaded PDF from the UI and index it at runtime for an interactive path.
*   Implement a fixed-size chunking strategy with configurable defaults. Start with a default chunk size in the 200 to 500 token range and a default overlap in the 10-20% range, but expose both values as parameters so they can be tuned.
*   Use the `sentence-transformers` library to convert text chunks into dense numerical vectors.
*   Store these vectors in a local `faiss` vector index together with metadata such as source filename, page number when available, and chunk ID.

**2. `src/retrieval.py` (Query-time Retrieval)**
*   Create a function that accepts a user query and vectorizes it using the same `sentence-transformers` embedding model.
*   Perform a similarity search against the FAISS index to retrieve the top-k most relevant chunks.
*   Treat `k` as a configurable default parameter rather than a fixed optimal value. Use a sensible initial default and document that it should be tuned based on the corpus, latency, and answer quality.

**3. `src/generation.py` (Augmented Output)**
*   Create a function that combines the retrieved context chunks and the user's original query into a single augmented prompt.
*   Set up a mock or Gemini API-based LLM call that takes this augmented prompt and returns a final grounded answer with citations.
*   Use the official Google GenAI Python SDK with `GEMINI_API_KEY` for authenticated requests.
*   Define citations using the retrieval metadata. At minimum, include source filename and chunk ID, and include page number when available.
*   If the retrieved evidence is weak or insufficient, instruct the system to say so rather than inventing facts.

**4. `app.py` (The User Interface)**
*   Build a simple Streamlit web application that demonstrates a real retrieval workflow for answering technical questions from engineering documents.
*   Include support for both ingestion modes:
*   A control to use the bundled default `data/document.pdf`.
*   A file uploader so the user can upload a PDF and trigger indexing in the app.
*   Include a text input for the user's query and display the final grounded answer along with the retrieved evidence snippets and citations.
*   Expose key retrieval settings such as top-k and, if practical, chunk size and overlap.

**5. `requirements.txt`**
*   Include all Python dependencies needed to run the demo locally.
*   Pin or constrain versions where appropriate to improve reproducibility.

**6. `README.md`**
*   Explain the project structure, setup steps, and how to run the app.
*   Describe how the indexing, retrieval, and generation stages work.
*   Document the configurable defaults for chunk size, overlap, and top-k retrieval.
*   Explain how to use both ingestion modes: the bundled default `data/document.pdf` and a user-uploaded PDF.

When writing the code, focus on clear reasoning, modular design, and concise comments that explain what the system is doing and why.

If you encounter errors during execution, you may retry automatically within a bounded repair loop of at most 2 iterations. Only make small, inspectable fixes. Do not silently change the project scope, dependencies, or architecture without explicitly reporting those changes in your final output."
